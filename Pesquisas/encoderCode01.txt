//ENCODER
// Encoder A
#define A 3
// Encoder B
#define B 4



void setup() {
  pinMode (A, INPUT);
  pinMode (B, INPUT);

  // Estado Anterior
  String EA = "I";

  if((A == 1) AND (B == 1)){
    EA = 1;
  }else{
    EA = 0;
  }

  // Direção
  String DIR = "I";
  // Posição
  int P = 0;

}


void loop () {
  //int sensorValue = analogRead(Pot_A0);
  //delay(200);
}
